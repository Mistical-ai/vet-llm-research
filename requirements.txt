# Compatibility installer for existing setup docs.
#
# Runtime dependencies are pinned in requirements-lock.txt so installs are
# reproducible. For tests and CI, install requirements-dev-lock.txt instead.
-r requirements-lock.txt
