# Node dependencies — reinstall with `npm install` (pre-bundle into the zip for reviewers).
node_modules/

# Local run artifacts, if any land here.
*.log

# Built reviewer zips (recreate with make-reviewer-package.ps1).
dist/

